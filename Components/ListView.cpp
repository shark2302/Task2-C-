//
// Created by e.kravchenko on 14.03.2021.
//
#include <typeinfo>
#include "ListView.h"

/*template<typename T>
ListView<T>::ListView(int x, int y, int w, int h, bool a, list<T> items) : WindowComponent(x, y, w, h, a) {
    _items = items;
}

template<typename T>
list<T> ListView<T>::getItems() {
    return _items;
}

template<typename T>
string ListView<T> ::getStringItems() {
    string res;
    for(auto iter = _items.begin(), i = 0; iter != _items.end(); iter++, i++)
    {
        res += to_string(i) + ")" + iter->getShortInfo() + "\n";
    }
    return res;
}*/

/*template<typename T>
string ListView<T>::getShortInfo() const {
    return "(ListView(" + to_string((int) typeid(T).name()) + ")";
}*/

